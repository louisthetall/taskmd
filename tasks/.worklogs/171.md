## 2026-02-20T10:00:00Z

Started documentation task 171 - Document missing CLI flags and commands.

**Approach:** Reading both CLI guide files and comparing against actual `--help` output from all commands. Will update both `apps/docs/guide/cli.md` (VitePress) and `docs/guides/cli-guide.md` (standalone) to fill all documentation gaps.

**Key gaps identified:**
- VitePress: Missing `todos` command, `next --quick-wins/--critical`, `web export`, `web start --readonly`
- Standalone: Missing many command sections (add, search, verify, status, context, worklog, import, spec, commit-msg, todos), plus missing flags on existing commands

## 2026-02-20T10:30:00Z

Completed all documentation updates.

**VitePress CLI guide (`apps/docs/guide/cli.md`):**
- Added `todos` to Quick Reference table
- Added full `todos` / `todos list` command section with all flags
- Added `next --quick-wins` and `next --critical` to next command examples
- Restructured web section into `web start` and `web export` subsections
- Added `web start --readonly` flag
- Added `web export` subcommand with `--output` and `--base-path` flags

**Standalone CLI guide (`docs/guides/cli-guide.md`):**
- Added `todos` to Quick Reference table
- Added full command sections for: add, search, verify, status, context, worklog, import, spec, commit-msg, todos, completion
- Added `next --quick-wins` and `next --critical` flags
- Added `graph --filter` flag with examples
- Added `get --context` flag to flags table
- Restructured web section into `web start` and `web export` subsections
- Added `web start --readonly` flag
- Added `web export` subcommand

All documentation follows existing style conventions (description, flags table, examples).
