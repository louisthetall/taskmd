## 2026-02-15T10:00:00Z

Implemented replacement of `sync_id` with `external_id` in sync writer.

**Changes:**
- `apps/cli/internal/sync/writer.go`: Changed `renderTaskFile` to write `external_id` instead of `sync_id` in frontmatter
- `apps/cli/internal/sync/engine_test.go`: Added `TestEngine_ExternalIDInTaskFiles` covering both create and update paths

**Notes:**
- `UpdateSyncedTaskFile` already preserves `external_id` because `taskfile.UpdateTaskFile` only modifies fields specified in the `UpdateRequest` -- `external_id` is not touched, so it persists through updates.
- No existing tests asserted on `sync_id` in rendered content, so no test updates were needed for existing tests.
- `sync_source` left as-is per task requirements.
- All 35 sync tests pass, plus full test suite.
