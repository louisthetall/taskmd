## 2026-02-28T16:30:00Z

Implemented effort filter toggle group in FilterBar.

**Approach:** Task 208 had already set up the `EFFORTS` constant, `EFFORT_COLORS`, `selectedEffort` state, URL param syncing, and filtering logic. The main gap was the FilterBar UI and wiring props through.

**Changes:**
- `FilterBar.tsx`: Added `selectedEffort`/`onToggleEffort` props and an "Effort" toggle group between Priority and Type, following the same pattern
- `TaskTable.tsx`: Wired effort props to FilterBar with URL sync (clears param when all selected)
- `filters.ts`: Changed effort from opt-in (`size > 0`) to match status/priority pattern (`size < EFFORTS.length`), so all efforts selected = show all
- `filters.test.ts`: Updated tests for new default state (all efforts selected)

**Completed:**
- [x] All 5 subtasks
- [x] All tests pass (182/182)
