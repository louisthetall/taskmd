## 2026-02-15T10:00:00Z

Replaced the board page tag filter pill list with a compact autocomplete/combobox search input.

**Approach:** Created a new `TagAutocomplete` component that provides:
- A text input with autocomplete dropdown for searching/selecting tags
- Selected tags displayed as removable chips inline with the input
- Full keyboard navigation (ArrowUp/Down, Enter to select, Escape to close)
- Edge case handling (no matches message, already-selected tags hidden from dropdown)

**Changes:**
- Created `apps/web/src/components/board/TagAutocomplete.tsx` - new combobox component
- Updated `apps/web/src/components/board/BoardFilterBar.tsx` - swapped PillRow for TagAutocomplete, removed unused TAG_ACTIVE constant

**Decisions:**
- Kept the component board-specific rather than making it a generic shared component, since it's only used in one place
- Only show unselected tags in the dropdown to keep it clean
- Used `onMouseDown preventDefault` on dropdown items to prevent input blur before click fires

**Completed:**
- [x] All 5 subtasks completed
- [x] TypeScript compiles cleanly
