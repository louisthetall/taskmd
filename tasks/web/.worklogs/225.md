## 2026-02-26T20:14:00Z

Implemented folder/group filter for the Next Up page.

**Approach:** Added a `<select>` dropdown to NextView that lists all unique groups from the tasks data (via useTasks hook). The selected group is passed as a `filter=group=<value>` query parameter to the backend's `/api/next` endpoint, which already supports this filtering. Both `limit` and `group` are synced to URL search params for persistence.

**Changes:**
- `apps/web/src/hooks/use-next.ts` — Added optional `group` parameter, builds URL with `filter=group=<value>` when set
- `apps/web/src/pages/NextPage.tsx` — Reads `limit` and `group` from URL search params (via `useSearchParams`), provides setter callbacks that update the URL
- `apps/web/src/components/next/NextView.tsx` — Added folder dropdown populated from task groups (via `useTasks` hook), placed next to the existing limit buttons
- `apps/web/src/components/next/NextView.test.tsx` — 8 tests covering dropdown rendering, group selection, and limit buttons
- `apps/web/src/pages/NextPage.test.tsx` — 8 tests covering URL param reading, defaults, and filter interaction

All 103 web tests pass.
