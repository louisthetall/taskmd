## 2026-02-17T10:00:00Z

Started documenting all missing CLI commands in the CLI guide.

**Approach:** Read each command's source file to extract flags, defaults, descriptions, and usage patterns. Document each in the same style as existing sections in `apps/docs/guide/cli.md`.

**Commands to document:** verify, add, search, context, status, worklog, import, spec, mcp, man
**Also:** Add show/update aliases to get/set sections, update Quick Reference table.

## 2026-02-17T10:15:00Z

Completed all documentation.

**What was done:**
- Added 11 new commands to the Quick Reference table (add, search, verify, status, context, worklog, import, spec, mcp, man, plus commit-msg was already there)
- Added dedicated sections for each command with description, flags tables, and usage examples
- Added alias notes for `show` (alias of `get`) and `update` (alias of `set`) as blockquotes in their parent sections
- All documentation follows the existing style: heading, description, code examples, flags table
- For `import`, documented both GitHub-specific and Jira-specific shortcut flags in separate tables
