# Worklog: 141 - Add `sync down` subcommand

## 2026-02-17T00:00:00Z

Starting implementation.

**Approach:** Follow the existing `web`/`web start` parent-subcommand pattern from `web.go`.
Convert `syncCmd` into a parent command (no `RunE`, no `Args: cobra.NoArgs`) and create
`syncDownCmd` as a subcommand that inherits all existing flags and the `runSync` logic.

**Plan:**
1. Remove `RunE` and `Args` from `syncCmd`, update help text
2. Create `syncDownCmd` with `Use: "down"`, move flags and `RunE: runSync` to it
3. Register `syncDownCmd` under `syncCmd`
4. Update tests to call `runSync` via `syncDownCmd` (or call `runSync` directly since it's the same function)
5. Update docs references from `taskmd sync` to `taskmd sync down`

## 2026-02-17T00:10:00Z

Implementation complete.

**Changes made:**
- `apps/cli/internal/cli/sync.go`: Converted `syncCmd` to parent command (removed `RunE`, `Args`), created `syncDownCmd` subcommand with all flags and `runSync` logic
- `apps/cli/internal/cli/sync_test.go`: Updated all test calls from `syncCmd` to `syncDownCmd`, added `TestSyncCommand_ParentShowsHelp` test
- `docs/guides/cli-guide.md`: Updated all `taskmd sync` references to `taskmd sync down`
- `apps/docs/guide/cli.md`: Updated all `taskmd sync` references to `taskmd sync down`
- `docs/.taskmd.yaml.example`: Updated comment to reference `taskmd sync down`

**Results:** All 8 sync tests pass, full test suite passes, linter clean.
