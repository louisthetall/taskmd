## 2026-02-17T12:00:00Z

Documented the `commit-msg` CLI command in `apps/docs/guide/cli.md`.

**Approach:** Read the source implementation (`apps/cli/internal/cli/commit_msg.go`) to understand all flags, defaults, behavior, and auto-detection logic, then wrote documentation matching the style of existing command sections.

**Completed:**
- [x] Added `commit-msg` to the Quick Reference table
- [x] Added dedicated `### commit-msg` section with description, flags table, and usage examples
- [x] Documented all flags (`--task-id`, `--type`, `--body`, `--short`) with defaults
- [x] Covered all key workflows: single task, custom type, body, short, auto-detect, git integration
- [x] Explained auto-detection behavior (inspects `git diff --cached` for `+status: completed`)
- [x] Documented multi-task subject line format
