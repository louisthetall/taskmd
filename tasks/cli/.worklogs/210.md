## 2026-02-25T00:00:00Z

Completed implementation of ULID ID strategy replacing UUID.

**What was done:**
- Added `GenerateULID` in `internal/nextid/nextid.go` — encodes 48-bit ms timestamp + 80-bit crypto random as Crockford Base32 (lowercase), with truncation support
- Removed `GenerateUUID` function entirely
- Updated CLI routing in `nextid.go` and `add.go` to use `"ulid"` strategy
- Updated validator to accept `"ulid"` and reject `"uuid"`
- Replaced UUID tests with comprehensive ULID tests (format, sortability, collision avoidance, uniqueness, length truncation, ordered batch)
- Updated `docs/taskmd_specification.md` and `docs/guides/cli-guide.md`
- Ran `make sync-spec` to propagate spec changes

**Verification:** `make test`, `make lint`, and `make e2e` all pass clean.
